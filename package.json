{
  "name": "js_refresh",
  "version": "1.0.0",
  "description": "simple sandbox workspace",
  "main": "index.js",
  "scripts": {
    "sass:watch": "sass --watch scss:css",
    "server": "browser-sync start --server --files css index.html scripts",
    "start": "concurrently 'npm run sass:watch' 'npm run server'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^2.26.7",
    "concurrently": "^5.1.0",
    "node-sass": "^4.13.1"
  }
}
